## Math
**// 1. Java 中所有数学运算相关的工具方法，都统一封装在 java.lang.Math 类中，该类是系统内置的，无需手动导包，直接使用即可**
```java
Math.abs(整数);//绝对值 int
Math.pow(整数, 2);//平方
Math.sqrt(整数);//根号  返回值类型固定是 double 必须 ≥ 0
```
**// 添加/更新操作**
```java